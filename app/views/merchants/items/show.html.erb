<style>
.header {
  padding: 30px;
  text-align: center;
  background: #B2BEB5;
  font-size: 30px;
}
</style>
<h2> <%= @merchant.name %> </h2>

<h3 class="header"><%= @item.name %></h3>
<%= image_tag @item_small %>
<p>Desciption: <%= @item.description %></p>
<p>Current Price: <%= number_to_currency(@item.unit_price/100.00) %> </p>
<%= button_to 'Update Item', "/merchants/#{@merchant.id}/items/#{@item.id}/edit", method: :get %>

<!--// As a visitor or an admin user
// When I visit the Merchant Item Show page (/merchants/:merchant_id/items/:item_id)
// I see a photo related to that item's name